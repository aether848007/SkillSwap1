spring:
  datasource:
    # Defaults assume a local Postgres at postgres/postgres123. Production overrides
    # via DATABASE_* env vars from the compose .env file.
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/skillswap}
    driver-class-name: org.postgresql.Driver
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres123}
    hikari:
      maximum-pool-size: 10
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
  h2:
    console:
      enabled: false
  flyway:
    enabled: true
    locations: classpath:db/migration
